//-----------------------
Процедура ПриОткрытии()
	Если ПустаяСтрока(Форма.Параметр)=0 Тогда
		ИспользоватьВладельца(Форма.Параметр);
	КонецЕсли;
КонецПроцедуры
//-----------------------
Функция ИнфСтр()
	Возврат "Расчетные счета контрагента "+Владелец.Наименование;
КонецФункции
//-----------------------
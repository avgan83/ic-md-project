// ********************
//
Процедура ОбработкаПроведения()

    ВыбратьСтроки(); 
	СуммаСлед = 0;
	Пока ПолучитьСтроку () = 1 Цикл
		Если СокрЛП(КодСтрокиДекларации.Код) = "21" Тогда
			СуммаСлед = СуммаНДС;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	ПромИтоги=СоздатьОбъект("Текст");
	ПромИтоги.Открыть(КаталогИБ()+"nds.txt");
	ПромИтоги.ЗаменитьСтроку(ДатаМесяц(ДатаДок),Строка(СуммаСлед));
	ПромИтоги.Записать(КаталогИБ()+"nds.txt");
КонецПроцедуры

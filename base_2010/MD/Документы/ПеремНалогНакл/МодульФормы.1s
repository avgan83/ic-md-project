Процедура ВводНового()
	Автор=глПользователь;
КонецПроцедуры	

Процедура КоличествоБланков()
	Если Число(НомерПо)<Число(НомерС) Тогда
		Если ПустаяСтрока(НомерПо)=0 Тогда
			Предупреждение("Номер ПО "+НомерПо+"меньше "+НомерС+"!");
		КонецЕсли;
	Иначе
		Колво=Число(НомерПо)-Число(НомерС)+1;
	КонецЕсли;
КонецПроцедуры



Процедура ПриЗаписи()
	//Сообщить(Режим);
КонецПроцедуры

